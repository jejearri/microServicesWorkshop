spring:
  application:
    name: configServer
#  profiles:
#    active: native    
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  instance:
    nonSecurePort: 8888